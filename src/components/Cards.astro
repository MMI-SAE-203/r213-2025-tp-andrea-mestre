---
const { ...offre } = Astro.props;
---

<div class="flex flex-col items-center justify-center m-7 mx-auto">
  <div
    class="flex flex-col items-center justify-center p-7 border-2 border-black"
  >
    <img class="w-96" src={offre.img} alt="" />
    <h3 class="text-2xl font-bold font-serif pt-5">{offre.nom_maison}</h3>
    <a
      class="mt-3 border-2 border-black bg-black px-4 py-2 rounded-md text-white"
      href={`/offres/${offre.id}`}>Voir plus</a
    >
  </div>
</div>
