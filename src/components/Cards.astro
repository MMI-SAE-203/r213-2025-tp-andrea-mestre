---
const { ...offre } = Astro.props;
---

<div class="flex flex-col items-center justify-center m-7 mx-auto">
  <div
    class="flex flex-col items-center justify-center m-7 mx-auto bg-gray-200 p-4 rounded-lg shadow-lg">
    <img class="w" src={offre.img} alt="" />
    <h3 class="text-2xl font-bold font-serif">{offre.nom_maison}</h3>
    <a href={`/offres/${offre.id}`}>Voir plus</a>
  </div>
</div>
